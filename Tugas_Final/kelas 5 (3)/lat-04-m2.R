# Menghitung akar kuadrat 
waktu <- 144
akar_waktu <- sqrt(waktu) 
akar_waktu

# Logaritma Ukuran File
ukuran_file <- 2048
log_file <- log(ukuran_file) 
log_file

# Nilai Mutlak Selisih Waktu Respons 
respon_v1 <- 320
respon_v2 <- 290
selisih_respon <- abs(respon_v1 - respon_v2) 
selisih_respon

# Fungsi Statistika Sederhana
memori <- c(512, 480, 530, 500)
min_memori <- min(memori) 
max_memori <- max(memori)
min_memori 
max_memori

# Rata-rata Jumlah Baris Kode per Modul 
baris_kode <- c(350, 420, 380, 400)
rata_kode <- mean(baris_kode) 
rata_kode

round(3.1415, digits = 2)
100:130
t <- c(74, 31, 95, 61, 76, 34, 23, 54, 96) 
mean(t)
median(t)
sort(t)
quantile(t)
summary(t)

x <- seq(1, 20, 0.1) 
y <- sin(x)
plot(x,y)

x<-seq(-4*pi,4*pi,length=500) 
y<-sin(x)
plot(x,y,type="l")
